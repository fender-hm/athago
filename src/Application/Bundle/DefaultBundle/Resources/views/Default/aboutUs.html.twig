{% extends '::layout.html.twig' %}

{% block slider %}
    {{ render(controller("ApplicationSliderBundle:Default:index", {'id': 2})) }}
{% endblock %}

{% block main_content %}
    <h1>{{ page.title|escape }}</h1>
    {{ page.content|raw }}
{% endblock %}